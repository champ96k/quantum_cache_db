name: quantum_cache_db_benchmark
description: "A new Flutter project."

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.4.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  quantum_cache_db:
    path: ../../

  hive: ^2.0.5
  hive_flutter: ^1.1.0
  sqflite: ^2.0.0+4
  path_provider: ^2.0.8
  sqlite3: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true
